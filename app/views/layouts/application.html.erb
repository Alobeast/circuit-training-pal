<!DOCTYPE html>
<html class="overflow-y-scroll h-full">
  <head>
    <title>CircuitTrainingPal</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload",
     defer: true %>
  </head>

  <body class="flex flex-col min-h-full bg-background text-body leading-normal
   font-sans" >

    <div id ="flash" class="fixed top-20 left-1/2 mx-auto flex flex-col
    items-center gap-3 max-w-full w-max py-0 px-4" >
      <%= render "layouts/flash" %>
    </div>
    <%= yield %>
  </body>
</html>
